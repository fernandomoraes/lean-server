var expect = require('chai').expect;
var lean = require('../index').instance();

describe('#route', () => {

	it('should return 200 when match route', () => {
		//TODO
	});

	it('should return 404 when not match route', () => {
		//TODO
	});
});